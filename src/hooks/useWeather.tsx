import axios from 'axios';
import { useEffect, useState } from 'react';

export const useWeather = () => {
    const fetchWeather = () => {
        
    }

    useEffect(() => {
        fetchWeather();
    }, []);
}